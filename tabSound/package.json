{
  "name": "tabsound",
  "version": "1.0.0",
  "description": "Browser extension to identify and control audio playback from any tab",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "package": "node build.js --production && zip -r tabsound-extension.zip dist/ -x '*.DS_Store'"
  },
  "keywords": [
    "browser-extension",
    "audio",
    "volume-control",
    "chrome-extension"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "fs-extra": "^11.1.1",
    "chokidar": "^3.5.3"
  }
}

